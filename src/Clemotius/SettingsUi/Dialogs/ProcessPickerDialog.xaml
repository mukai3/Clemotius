<ui:FluentWindow
    x:Class="Clemotius.SettingsUi.Dialogs.ProcessPickerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="アプリの選択"
    Width="460"
    Height="540"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Title="アプリの選択"
            ShowMaximize="False"
            ShowMinimize="False" />

        <StackPanel Grid.Row="1" Margin="16,4,16,8">
            <TextBlock
                Margin="0,0,0,8"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                TextWrapping="Wrap"
                Text="実行中のアプリから選択します。チェックを付けたアプリが対象に追加されます。" />
            <ui:TextBox
                x:Name="Filter"
                Icon="{ui:SymbolIcon Search24}"
                PlaceholderText="絞り込み（プロセス名・ウィンドウタイトル）"
                TextChanged="OnFilterChanged" />
        </StackPanel>

        <Border
            Grid.Row="2"
            Margin="16,0"
            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <ListView
                x:Name="List"
                BorderThickness="0"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Margin="4,2" IsChecked="{Binding IsSelected, Mode=TwoWay}">
                            <StackPanel Margin="6,0,0,0">
                                <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                                <TextBlock
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="{Binding Title}"
                                    TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </CheckBox>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Grid Grid.Row="3" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:Button
                Grid.Column="1"
                MinWidth="90"
                Appearance="Primary"
                Click="OnOk"
                Content="OK" />
            <ui:Button
                Grid.Column="3"
                MinWidth="90"
                Click="OnCancel"
                Content="キャンセル" />
        </Grid>
    </Grid>
</ui:FluentWindow>
